
import Foundation

import UIKit

class MSChatCell: UICollectionViewCell {
var aimage_tag: Int = 0
private var recognitionDetailStr: String?




    @IBOutlet weak var icon: UIImageView!
    @IBOutlet weak var namelabel: UILabel!

@discardableResult
 func navigationMarkOriginPlayer(mainSort: String!, numberLabel: String!, decibelSetting: Float) -> String! {
    var nameU: Bool = false
    var table7: String! = String(cString: [101,104,105,103,104,0], encoding: .utf8)!
    var replaceU: String! = String(cString: [115,117,98,118,105,101,119,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &replaceU) { pointer in
          _ = pointer.pointee
   }
   while (replaceU.count == 5) {
      replaceU.append("\(table7.count)")
      break
   }
   while (table7.count > replaceU.count) {
       var navn: Double = 3.0
       var d_animationq: String! = String(cString: [109,97,115,107,101,100,0], encoding: .utf8)!
       _ = d_animationq
         d_animationq = "\(Int(navn))"
         d_animationq = "\(3)"
      replaceU = "\(((nameU ? 4 : 1) - Int(navn)))"
      break
   }
   if 5 <= replaceU.count {
      table7 = "\((replaceU.count ^ (nameU ? 4 : 5)))"
   }
   repeat {
       var labeli: Float = 2.0
       _ = labeli
       var navl: Double = 0.0
      repeat {
         labeli *= Float(1 / (Swift.max(Int(navl), 6)))
         if 2968266.0 == labeli {
            break
         }
      } while (2968266.0 == labeli) && (navl <= Double(labeli))
          var main_j5: Int = 4
         withUnsafeMutablePointer(to: &main_j5) { pointer in
    
         }
          var register_7lr: String! = String(cString: [112,101,101,107,0], encoding: .utf8)!
          var settingp: Float = 4.0
          _ = settingp
         navl *= Double(main_j5)
         register_7lr = "\(main_j5 % (Swift.max(1, register_7lr.count)))"
         settingp *= Float(Int(navl))
         navl /= Swift.max(Double(2 * Int(labeli)), 1)
      if labeli <= 5.81 {
         navl += Double(Int(labeli))
      }
         labeli -= Float(Int(labeli) | 2)
         navl += Double(Int(labeli) ^ Int(navl))
      nameU = navl > 89.47
      if nameU ? !nameU : nameU {
         break
      }
   } while (!nameU) && (nameU ? !nameU : nameU)
      table7.append("\(replaceU.count | 1)")
   return table7

}





    
    override func awakeFromNib() {

         let ipdopdBinhex: String! = navigationMarkOriginPlayer(mainSort:String(cString: [111,95,57,56,95,112,117,110,99,104,0], encoding: .utf8)!, numberLabel:String(cString: [101,110,99,97,112,115,117,108,97,116,101,100,0], encoding: .utf8)!, decibelSetting:8140.0)

      let ipdopdBinhex_len = ipdopdBinhex?.count ?? 0
     var tmp_i_62 = Int(ipdopdBinhex_len)
     tmp_i_62 -= 26
      print(ipdopdBinhex)

_ = ipdopdBinhex


       var emptyq: Double = 0.0
   withUnsafeMutablePointer(to: &emptyq) { pointer in
    
   }
    var instance9: String! = String(cString: [114,101,112,97,105,110,116,0], encoding: .utf8)!
    _ = instance9
   for _ in 0 ..< 2 {
       var y_height_: [String: Any]! = [String(cString: [108,119,115,115,112,110,0], encoding: .utf8)!:832, String(cString: [108,111,119,99,111,109,112,0], encoding: .utf8)!:489, String(cString: [116,114,97,110,115,109,105,116,116,101,100,0], encoding: .utf8)!:826]
         y_height_ = ["\(y_height_.values.count)": y_height_.keys.count % (Swift.max(3, 8))]
      for _ in 0 ..< 2 {
         y_height_["\(y_height_.values.count)"] = y_height_.count - y_height_.values.count
      }
          var numlabelx: Double = 4.0
         y_height_ = ["\(y_height_.values.count)": y_height_.keys.count >> (Swift.min(labs(3), 4))]
         numlabelx += Double(y_height_.values.count)
      instance9 = "\(y_height_.values.count)"
   }

      emptyq /= Swift.max(Double(2 & Int(emptyq)), 4)
        super.awakeFromNib()
      emptyq -= Double(instance9.count)
        
        icon.layer.cornerRadius = 20
   repeat {
      emptyq *= Double(Int(emptyq) % 3)
      if 183262.0 == emptyq {
         break
      }
   } while (183262.0 == emptyq) && (2 > instance9.count)
        icon.layer.masksToBounds = true
        icon.layer.borderWidth = 2
        
        
    }

}
