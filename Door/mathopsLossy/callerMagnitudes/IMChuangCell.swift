
import Foundation

import UIKit

class IMChuangCell: UICollectionViewCell {
private var bufferResponse_Array: [Any]!
var ableMessage_map: [String: Any]!




    @IBOutlet weak var sepakImage: UIImageView!

@discardableResult
 func primeRateResignPriceCellTerminateTableView(screenDit: Double, actionLishi: String!, picPhoto: [String: Any]!) -> UITableView! {
    var fixedL: [String: Any]! = [String(cString: [112,97,100,0], encoding: .utf8)!:8, String(cString: [111,109,105,116,116,105,110,103,0], encoding: .utf8)!:706]
    var modele: Bool = false
       var pictureL: String! = String(cString: [121,118,99,111,110,102,105,103,0], encoding: .utf8)!
       var modelI: Double = 3.0
       var img4: Double = 0.0
       _ = img4
         modelI -= Double(pictureL.count + 2)
         pictureL = "\(3)"
         img4 += Double(1)
      fixedL = ["\(fixedL.count)": 2 / (Swift.max(4, fixedL.keys.count))]
       var eveantB: Double = 0.0
       _ = eveantB
       var bottomS: Bool = false
      withUnsafeMutablePointer(to: &bottomS) { pointer in
             _ = pointer.pointee
      }
      if 1.8 < eveantB || (1.8 + eveantB) < 1.71 {
          var viiewr: Double = 2.0
         withUnsafeMutablePointer(to: &viiewr) { pointer in
    
         }
         eveantB += (Double(Int(viiewr) ^ (bottomS ? 5 : 2)))
      }
      for _ in 0 ..< 2 {
          var endu: Bool = false
          _ = endu
          var navw: String! = String(cString: [99,111,109,98,105,110,101,114,0], encoding: .utf8)!
          var btnm: String! = String(cString: [101,116,104,101,114,116,117,112,108,105,115,0], encoding: .utf8)!
          var settingi: Float = 3.0
          var viiewW: String! = String(cString: [118,111,99,97,98,95,121,95,52,51,0], encoding: .utf8)!
         bottomS = navw.count >= btnm.count
         endu = 93.10 <= eveantB
         settingi *= Float(btnm.count)
         viiewW.append("\(3)")
      }
      while (eveantB > 4.19) {
         eveantB -= (Double((bottomS ? 2 : 1) >> (Swift.min(labs(Int(eveantB)), 2))))
         break
      }
         eveantB /= Swift.max(Double(3 | Int(eveantB)), 5)
      repeat {
          var randomw: Double = 2.0
         eveantB += Double(Int(eveantB) - Int(randomw))
         if 1235229.0 == eveantB {
            break
         }
      } while (!bottomS) && (1235229.0 == eveantB)
         bottomS = bottomS && 68.66 <= eveantB
      fixedL = ["\(fixedL.count)": 3]
   if !modele {
       var homeo: String! = String(cString: [99,111,109,112,108,101,116,101,100,0], encoding: .utf8)!
       var style2: Int = 2
       var nicknames: String! = String(cString: [114,97,110,107,0], encoding: .utf8)!
       var indexr: Double = 2.0
       var alamofireA: Bool = false
       _ = alamofireA
         homeo = "\(2 - nicknames.count)"
      if (style2 / 4) < 4 && (style2 / (Swift.max(4, 4))) < 4 {
         homeo.append("\(style2)")
      }
       var userw: String! = String(cString: [98,117,102,101,114,0], encoding: .utf8)!
      if userw.count > 4 {
         userw = "\(homeo.count)"
      }
         alamofireA = (homeo.count >> (Swift.min(3, labs(style2)))) == 59
       var vipm: Double = 2.0
       var loadingR: Double = 3.0
      withUnsafeMutablePointer(to: &loadingR) { pointer in
    
      }
         alamofireA = 57.6 > loadingR && 51 > userw.count
         userw = "\(1)"
      for _ in 0 ..< 1 {
         vipm *= Double(Int(vipm) % 1)
      }
          var nickname8: String! = String(cString: [97,114,99,104,105,118,105,110,103,0], encoding: .utf8)!
         userw.append("\(userw.count + 1)")
         nickname8.append("\(Int(indexr))")
      for _ in 0 ..< 1 {
          var socketM: Double = 3.0
          var e_animationA: String! = String(cString: [119,114,105,116,101,108,111,99,107,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &e_animationA) { pointer in
    
         }
          var searchd: Double = 3.0
         alamofireA = (Double(e_animationA.count) - searchd) <= 98.34
         socketM /= Swift.max(Double(3 >> (Swift.min(5, labs(Int(indexr))))), 3)
      }
      if 2 >= (style2 / 1) || (homeo.count / 1) >= 4 {
         homeo.append("\(Int(vipm))")
      }
          var url3: Double = 2.0
         userw.append("\(nicknames.count << (Swift.min(labs(3), 5)))")
         url3 /= Swift.max(2, Double(Int(indexr) >> (Swift.min(4, labs(Int(vipm))))))
      repeat {
         loadingR *= Double(homeo.count % (Swift.max(7, style2)))
         if loadingR == 3241820.0 {
            break
         }
      } while (5.26 < loadingR || 5.82 < (5.26 - loadingR)) && (loadingR == 3241820.0)
         alamofireA = userw.count < 22
      fixedL = [homeo: 1]
   }
     var addressDraw: Bool = true
     let headerEnd: UIImageView! = UIImageView(image:UIImage(named:String(cString: [99,112,105,97,0], encoding: .utf8)!), highlightedImage:UIImage(named:String(cString: [100,97,116,97,98,97,115,101,0], encoding: .utf8)!))
     let eveantPrefix_sw: Bool = true
     var enabledOne: Double = 3919.0
    var abortReasonDir = UITableView(frame:CGRect.zero)
    abortReasonDir.delegate = nil
    abortReasonDir.dataSource = nil
    abortReasonDir.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    abortReasonDir.alpha = 0.5;
    abortReasonDir.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    abortReasonDir.frame = CGRect(x: 301, y: 102, width: 0, height: 0)
    headerEnd.frame = CGRect(x: 176, y: 82, width: 0, height: 0)
    headerEnd.alpha = 0.2;
    headerEnd.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    headerEnd.animationRepeatCount = 3
    headerEnd.image = UIImage(named:String(cString: [110,111,116,105,102,105,99,97,116,105,111,110,0], encoding: .utf8)!)
    headerEnd.contentMode = .scaleAspectFit
    
    var headerEndFrame = headerEnd.frame
    headerEndFrame.size = CGSize(width: 286, height: 239)
    headerEnd.frame = headerEndFrame
    if headerEnd.alpha > 0.0 {
         headerEnd.alpha = 0.0
    }
    if headerEnd.isHidden {
         headerEnd.isHidden = false
    }
    if !headerEnd.isUserInteractionEnabled {
         headerEnd.isUserInteractionEnabled = true
    }

         var _u_27 = Int(enabledOne)
     if _u_27 <= 45 {
          var r_5: Int = 0
     let d_17 = 1
     if _u_27 > d_17 {
         _u_27 = d_17

     }
     if _u_27 <= 0 {
         _u_27 = 1

     }
     for m_50 in 0 ..< _u_27 {
         r_5 += m_50
          if m_50 > 0 {
          _u_27 -= m_50
     break

     }
     var s_83 = r_5
              break

     }
     }

    
    var abortReasonDirFrame = abortReasonDir.frame
    abortReasonDirFrame.size = CGSize(width: 272, height: 216)
    abortReasonDir.frame = abortReasonDirFrame
    if abortReasonDir.alpha > 0.0 {
         abortReasonDir.alpha = 0.0
    }
    if abortReasonDir.isHidden {
         abortReasonDir.isHidden = false
    }
    if !abortReasonDir.isUserInteractionEnabled {
         abortReasonDir.isUserInteractionEnabled = true
    }

    return abortReasonDir

}





    override func awakeFromNib() {

         var metasoundInodes: UITableView! = primeRateResignPriceCellTerminateTableView(screenDit:3053.0, actionLishi:String(cString: [97,99,117,116,111,102,102,0], encoding: .utf8)!, picPhoto:[String(cString: [112,97,114,115,101,114,115,0], encoding: .utf8)!:155, String(cString: [118,105,115,97,0], encoding: .utf8)!:954, String(cString: [100,101,109,97,116,101,114,105,97,108,105,122,101,0], encoding: .utf8)!:178])

      if metasoundInodes != nil {
          self.addSubview(metasoundInodes)
          let metasoundInodes_tag = metasoundInodes.tag
     var _u_32 = Int(metasoundInodes_tag)
     var v_60: Int = 0
     let s_38 = 1
     if _u_32 > s_38 {
         _u_32 = s_38

     }
     if _u_32 <= 0 {
         _u_32 = 1

     }
     for z_22 in 0 ..< _u_32 {
         v_60 += z_22
          if z_22 > 0 {
          _u_32 -= z_22
     break

     }
          _u_32 += 64
         break

     }
      }
      else {
          print("metasoundInodes is nil")      }

withUnsafeMutablePointer(to: &metasoundInodes) { pointer in
    
}


       var eveanth: Float = 5.0
    var refresh4: String! = String(cString: [103,101,116,120,118,97,114,0], encoding: .utf8)!
   withUnsafeMutablePointer(to: &refresh4) { pointer in
          _ = pointer.pointee
   }
    var currentt: [String: Any]! = [String(cString: [99,108,116,111,115,116,114,0], encoding: .utf8)!:UILabel(frame:CGRect.zero)]
   repeat {
       var sliderP: String! = String(cString: [114,116,99,115,116,97,116,115,0], encoding: .utf8)!
      withUnsafeMutablePointer(to: &sliderP) { pointer in
    
      }
       var stringd: Bool = false
       var sizelabeln: Float = 5.0
      while (1.11 >= (sizelabeln + Float(sliderP.count)) && 1.11 >= (Float(sliderP.count) + sizelabeln)) {
          var style6: String! = String(cString: [98,108,111,111,109,0], encoding: .utf8)!
          _ = style6
         sizelabeln /= Swift.max(Float(1 | Int(sizelabeln)), 2)
         style6 = "\(Int(sizelabeln))"
         break
      }
         stringd = sliderP.count < Int(sizelabeln)
         sliderP = "\(sliderP.count / (Swift.max(3, 10)))"
          var bufferB: String! = String(cString: [115,112,111,116,0], encoding: .utf8)!
         withUnsafeMutablePointer(to: &bufferB) { pointer in
    
         }
         sizelabeln -= (Float(sliderP.count * (stringd ? 5 : 3)))
         bufferB = "\(((String(cString:[114,0], encoding: .utf8)!) == sliderP ? sliderP.count : (stringd ? 4 : 1)))"
         sizelabeln += (Float((stringd ? 4 : 2) * Int(sizelabeln)))
      for _ in 0 ..< 2 {
         sizelabeln -= Float(sliderP.count)
      }
      repeat {
          var readI: String! = String(cString: [98,111,116,104,0], encoding: .utf8)!
          var string_: [String: Any]! = [String(cString: [98,117,110,100,108,101,0], encoding: .utf8)!:true]
         stringd = sizelabeln <= 72.47
         readI = "\(3 >> (Swift.min(5, readI.count)))"
         string_ = [sliderP: sliderP.count]
         if stringd ? !stringd : stringd {
            break
         }
      } while (stringd ? !stringd : stringd) && (sizelabeln >= 4.32)
         sliderP = "\(((stringd ? 2 : 3) / (Swift.max(10, Int(sizelabeln)))))"
       var holderlabelZ: [Any]! = [[String(cString: [110,111,114,109,97,108,105,122,101,0], encoding: .utf8)!:391, String(cString: [108,111,110,103,105,116,117,100,101,0], encoding: .utf8)!:272]]
      withUnsafeMutablePointer(to: &holderlabelZ) { pointer in
             _ = pointer.pointee
      }
       var come6: [Any]! = [String(cString: [112,101,101,114,115,0], encoding: .utf8)!, String(cString: [115,111,108,118,101,100,0], encoding: .utf8)!]
         holderlabelZ = [(3 * (stringd ? 5 : 3))]
         come6 = [come6.count]
      eveanth *= Float(sliderP.count)
      if eveanth == 4830636.0 {
         break
      }
   } while (eveanth == 4830636.0) && (1 <= (refresh4.count - 3) && (3.34 + eveanth) <= 4.93)
   repeat {
      refresh4 = "\(refresh4.count)"
      if (String(cString:[117,57,95,102,54,0], encoding: .utf8)!) == refresh4 {
         break
      }
   } while (refresh4.count >= 3) && ((String(cString:[117,57,95,102,54,0], encoding: .utf8)!) == refresh4)

      refresh4.append("\(Int(eveanth))")
      refresh4 = "\(refresh4.count)"
        super.awakeFromNib()
      refresh4.append("\(Int(eveanth) + 1)")
      eveanth *= Float(currentt.count ^ 2)
        
        sepakImage.layer.borderWidth = 3
   while (4 > (currentt.values.count >> (Swift.min(labs(3), 5)))) {
      currentt = ["\(currentt.values.count)": 2]
      break
   }
   while (currentt["\(currentt.values.count)"] == nil) {
      currentt["\(currentt.values.count)"] = currentt.count & 3
      break
   }
        sepakImage.layer.cornerRadius = 16
        sepakImage.layer.masksToBounds = true
    }

}
