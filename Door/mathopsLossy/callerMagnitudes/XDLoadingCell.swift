
import Foundation

import UIKit

class XDLoadingCell: UICollectionViewCell {
private var avatarsSum: Int = 0
private var delete_bsOneImage_str: String!




    @IBOutlet weak var Icon: UIImageView!
    @IBOutlet weak var bgImage: UIImageView!

@discardableResult
 func borderFeedbackIdentityUnknownScrollView(regionSafe: [String: Any]!, queryStatus: Double, loginDelete_a6: Float) -> UIScrollView! {
    var addressX: Float = 1.0
    _ = addressX
    var saveK: String! = String(cString: [119,101,105,103,104,116,0], encoding: .utf8)!
      saveK.append("\(saveK.count)")
      saveK = "\(saveK.count ^ Int(addressX))"
   repeat {
       var recordingv4: Double = 5.0
       _ = recordingv4
      repeat {
          var buffer9: Double = 2.0
         withUnsafeMutablePointer(to: &buffer9) { pointer in
                _ = pointer.pointee
         }
          var emptyU: String! = String(cString: [114,101,117,112,108,111,97,100,101,100,0], encoding: .utf8)!
          _ = emptyU
          var q_playerc: Int = 1
         withUnsafeMutablePointer(to: &q_playerc) { pointer in
                _ = pointer.pointee
         }
          var normalk: Double = 2.0
         withUnsafeMutablePointer(to: &normalk) { pointer in
                _ = pointer.pointee
         }
         recordingv4 /= Swift.max(Double(emptyU.count), 3)
         buffer9 += Double(q_playerc)
         q_playerc >>= Swift.min(1, labs(3 & q_playerc))
         normalk *= Double(q_playerc)
         if recordingv4 == 1980257.0 {
            break
         }
      } while ((recordingv4 * recordingv4) >= 3.69) && (recordingv4 == 1980257.0)
      while ((1.53 + recordingv4) <= 4.94) {
         recordingv4 /= Swift.max(1, Double(2))
         break
      }
         recordingv4 += Double(Int(recordingv4))
      saveK.append("\(3 << (Swift.min(5, labs(Int(recordingv4)))))")
      if (String(cString:[51,53,118,98,106,119,51,97,101,0], encoding: .utf8)!) == saveK {
         break
      }
   } while ((String(cString:[51,53,118,98,106,119,51,97,101,0], encoding: .utf8)!) == saveK) && (5 <= (2 << (Swift.min(2, saveK.count))) || 2 <= (saveK.count % 2))
      saveK.append("\(1)")
     var responseAnimation: String! = String(cString: [104,97,114,100,116,104,114,101,115,104,95,115,95,51,56,0], encoding: .utf8)!
     var itemdataRing: UIView! = UIView()
     var socketBasic: [Any]! = [872, 696, 768]
    var predefinedSimplifyDuplicated:UIScrollView! = UIScrollView()
    predefinedSimplifyDuplicated.frame = CGRect(x: 102, y: 185, width: 0, height: 0)
    predefinedSimplifyDuplicated.alpha = 1.0;
    predefinedSimplifyDuplicated.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    predefinedSimplifyDuplicated.alwaysBounceVertical = false
    predefinedSimplifyDuplicated.alwaysBounceHorizontal = true
    predefinedSimplifyDuplicated.showsVerticalScrollIndicator = false
    predefinedSimplifyDuplicated.showsHorizontalScrollIndicator = false
    predefinedSimplifyDuplicated.delegate = nil
    predefinedSimplifyDuplicated.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    itemdataRing.frame = CGRect(x: 262, y: 169, width: 0, height: 0)
    itemdataRing.alpha = 0.5;
    itemdataRing.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    
    var itemdataRingFrame = itemdataRing.frame
    itemdataRingFrame.size = CGSize(width: 287, height: 102)
    itemdataRing.frame = itemdataRingFrame
    if itemdataRing.alpha > 0.0 {
         itemdataRing.alpha = 0.0
    }
    if itemdataRing.isHidden {
         itemdataRing.isHidden = false
    }
    if !itemdataRing.isUserInteractionEnabled {
         itemdataRing.isUserInteractionEnabled = true
    }

    predefinedSimplifyDuplicated.addSubview(itemdataRing)

    
    var predefinedSimplifyDuplicatedFrame = predefinedSimplifyDuplicated.frame
    predefinedSimplifyDuplicatedFrame.size = CGSize(width: 107, height: 52)
    predefinedSimplifyDuplicated.frame = predefinedSimplifyDuplicatedFrame
    if predefinedSimplifyDuplicated.isHidden {
         predefinedSimplifyDuplicated.isHidden = false
    }
    if predefinedSimplifyDuplicated.alpha > 0.0 {
         predefinedSimplifyDuplicated.alpha = 0.0
    }
    if !predefinedSimplifyDuplicated.isUserInteractionEnabled {
         predefinedSimplifyDuplicated.isUserInteractionEnabled = true
    }

    return predefinedSimplifyDuplicated

}





    
    override func awakeFromNib() {

         var notifierCwrsi: UIScrollView! = borderFeedbackIdentityUnknownScrollView(regionSafe:[String(cString: [114,101,115,116,0], encoding: .utf8)!:208, String(cString: [110,111,116,105,102,105,101,100,0], encoding: .utf8)!:868, String(cString: [110,117,109,98,101,114,95,54,95,50,51,0], encoding: .utf8)!:699], queryStatus:5887.0, loginDelete_a6:7998.0)

      if notifierCwrsi != nil {
          let notifierCwrsi_tag = notifierCwrsi.tag
     var temp_i_4 = Int(notifierCwrsi_tag)
     switch temp_i_4 {
          case 0:
          temp_i_4 *= 30
          temp_i_4 -= 87
     break
          case 100:
          temp_i_4 -= 57
          var e_66 = 1
     let y_34 = 0
     if temp_i_4 > y_34 {
         temp_i_4 = y_34
     }
     while e_66 < temp_i_4 {
         e_66 += 1
     var s_74 = e_66
          if s_74 == 203 {
          }
         break
     }
     break
          case 13:
          temp_i_4 /= 57
          if temp_i_4 == 808 {
          temp_i_4 /= 94
          }
     break
          case 9:
          temp_i_4 *= 32
          temp_i_4 += 64
     break
          case 97:
          temp_i_4 /= 93
          temp_i_4 /= 28
     break
          case 18:
          if temp_i_4 < 313 {
          temp_i_4 *= 93
     }
     break
     default:()

     }
          self.addSubview(notifierCwrsi)
      }

withUnsafeMutablePointer(to: &notifierCwrsi) { pointer in
        _ = pointer.pointee
}


       var awakeH: Double = 3.0
    var monthE: Double = 2.0
    _ = monthE
    var presentj: String! = String(cString: [115,121,110,99,109,97,114,107,101,114,0], encoding: .utf8)!
   while ((monthE / (Swift.max(Double(presentj.count), 7))) < 4.34 || 2.62 < (4.34 / (Swift.max(7, monthE)))) {
      monthE -= Double(Int(awakeH) % (Swift.max(presentj.count, 9)))
      break
   }
   repeat {
      awakeH -= Double(1 | Int(monthE))
      if 3946790.0 == awakeH {
         break
      }
   } while ((awakeH + monthE) < 2.4 || (awakeH + monthE) < 2.4) && (3946790.0 == awakeH)

   for _ in 0 ..< 3 {
      presentj.append("\(Int(monthE) & Int(awakeH))")
   }
      awakeH -= Double(3)
        super.awakeFromNib()
        
   for _ in 0 ..< 2 {
      presentj = "\(1 | presentj.count)"
   }
    }

}
